# Example environment variables for Docker deployment
# Copy this to .env.docker and fill in your values

# Database Configuration
DB_HOST=your-database-host
DB_USER=your-database-user
DB_PASSWORD=your-database-password
DB_NAME=your-database-name

# Authentication
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-nextauth-secret-key-generate-with-openssl-rand-hex-32
AZURE_AD_CLIENT_ID=your-azure-ad-client-id
AZURE_AD_CLIENT_SECRET=your-azure-ad-client-secret
AZURE_AD_TENANT_ID=your-azure-ad-tenant-id

# Storage Configuration
# For local storage (development)
STORAGE_TYPE=local
STORAGE_LOCAL_PATH=./storage

# For S3 storage (production)
# STORAGE_TYPE=s3
# S3_BUCKET_NAME=your-s3-bucket
# S3_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key

# OpenAI Configuration
OPENAI_API_KEY=your-openai-api-key

# Queue System Token
QUEUE_SYSTEM_TOKEN=your-queue-system-token-generate-with-openssl-rand-hex-32

# Application Configuration
NODE_ENV=production
PORT=3000